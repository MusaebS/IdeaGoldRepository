"""Streamlit UI package for the Idea Gold Scheduler.

``app.py`` stays the entry point (``streamlit run app.py``); these modules
hold the state management, reusable editors, configuration tabs, and results
rendering it composes.
"""
